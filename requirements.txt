# Vercel root requirements - LITE mode (Pillow only, fits in serverless)
fastapi==0.115.0
python-multipart==0.0.9
pillow==10.4.0
numpy==1.26.4
supabase==2.8.1
pydantic==2.9.2
requests==2.32.3
# Monitoring opt-in : si SENTRY_DSN est defini en env, le SDK est utilise.
# Si non defini, aucun cout (l'import echoue silencieusement).
sentry-sdk[fastapi]==2.18.0
